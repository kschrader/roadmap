%h1 Listing bundles

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @bundles.each do |bundle|
    %tr
      %td= bundle.name
      %td= link_to 'Show', bundle
      %td= link_to 'Edit', edit_bundle_path(bundle)
      %td= link_to 'Destroy', bundle, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Bundle', new_bundle_path
